<view class="goods-item">
  <image src="{{image}}"></image>
  <view class="goods-info-box">
    <view class="goods-title">{{title}}</view>
    <view class="goods-tools">
      <view class="goods-add" bindtap="onAdd" data-item="{{item}}">+</view>
    </view>
  </view>
</view>